config:
    aws:region: ap-northeast-1
    aws-k8s-vpc-architecture:vpcName: production-eks-vpc
    aws-k8s-vpc-architecture:vpcCidr: 10.0.0.0/16
    aws-k8s-vpc-architecture:azCount: "3"
    aws-k8s-vpc-architecture:clusterName: prod-cluster-v1

    # vpn certificate arn, maybe need --secret
    aws-k8s-vpc-architecture:vpnCertArn: "a virtual certificate arn"

    # VPN cidr block
    aws-k8s-vpc-architecture:vpnClientCidr: 10.100.0.0/22
